[defaults]
# YAML-shaped task results via the default callback (ansible-core 2.13+).
# Do not set stdout_callback=yaml — that name resolves to removed community.general.yaml.
stdout_callback = ansible.builtin.default
callback_result_format = yaml
