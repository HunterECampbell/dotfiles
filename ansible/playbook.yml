---
- name: Set up Pop!_OS workstation
  hosts: localhost
  roles:
    - apt-packages
    - chezmoi-setup
    - custom-dirs
    - flatpak-apps
    - discord-settings
    - role: zoom-settings
      when: "install_profile | default('all') in ['work','all']"
    - gnome-settings
    - snap-apps
