---
- name: Install common APT packages
  ansible.builtin.apt:
    name:
      - anacron
      - clamav
      - clamav-daemon
      - code
      - cron
      - python3-psutil
      - simplescreenrecorder
      - slack-desktop
    state: present
    update_cache: yes
  become: yes
